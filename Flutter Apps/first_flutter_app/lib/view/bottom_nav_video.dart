import 'package:flutter/material.dart';

class BottomNavVideo extends StatelessWidget {
  const BottomNavVideo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}